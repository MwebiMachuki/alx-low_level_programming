#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: input digit
 * Return: 1 if digit (0 to 9), 0 if otherwise
 */
int _isdigit(int c)
{
	if (c >= 58 && c <= 67)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
